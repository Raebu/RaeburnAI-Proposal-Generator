# Required only when live AI generation is enabled. Leave empty to use the
# clearly-labelled deterministic fallback.
OPENAI_API_KEY=
# Optional
OPENAI_MODEL=gpt-4.1-mini
RATE_LIMIT_REQUESTS_PER_MINUTE=20
